[package]
name = "kinodb-ingest"
version = "0.1.0"
edition = "2021"
description = "Format ingesters for kinodb — reads RLDS, LeRobot, HDF5 into .kdb"
license = "MIT OR Apache-2.0"

[dependencies]
kinodb-core = { path = "../kinodb-core" }
hdf5 = "0.8"
ndarray = "0.15"
