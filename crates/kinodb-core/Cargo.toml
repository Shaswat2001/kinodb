[package]
name = "kinodb-core"
version = "0.1.0"
edition = "2021"
description = "Core storage engine for kinodb — a trajectory database for robot learning"
license = "MIT OR Apache-2.0"

[dependencies]
memmap2 = "0.9"
image = { version = "0.25", default-features = false, features = ["jpeg"] }
